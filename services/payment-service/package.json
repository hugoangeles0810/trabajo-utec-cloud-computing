{
  "name": "@gamarriando/payment-service",
  "version": "1.0.0",
  "description": "Payment processing microservice for Gamarriando marketplace",
  "main": "main.py",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8003",
    "build": "echo 'Building payment service...'",
    "test": "pytest",
    "lint": "flake8 app/",
    "format": "black app/",
    "deploy": "serverless deploy",
    "deploy:dev": "serverless deploy --stage dev",
    "deploy:prod": "serverless deploy --stage prod",
    "migrate": "python app/db_migrations.py migrate",
    "migrate:create": "python app/db_migrations.py create",
    "migrate:reset": "python app/db_migrations.py reset"
  },
  "dependencies": {},
  "devDependencies": {
    "serverless": "^3.38.0",
    "serverless-python-requirements": "^6.0.0"
  },
  "keywords": [
    "payment",
    "stripe",
    "paypal",
    "microservice",
    "fastapi",
    "aws",
    "lambda"
  ],
  "author": "Gamarriando Team",
  "license": "MIT"
}
