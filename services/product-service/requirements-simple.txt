# Simple requirements for Gamarriando Product Service
# Only essential dependencies to avoid Lambda packaging issues

# AWS SDK (already available in Lambda runtime)
# boto3==1.34.0

# Basic utilities
python-dotenv==1.0.0
